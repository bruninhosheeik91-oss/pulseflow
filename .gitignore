node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# WhatsApp session data (WPPConnect) — never versioned
server/tokens/
server/node_modules/

# Runtime state do backend (monitor-config, contas) — never versioned
server/data/

# Cache persistente de grupos por sessão (snapshot em disco)
server/groups-cache/

.vercel
.env*
